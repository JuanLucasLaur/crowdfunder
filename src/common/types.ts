export interface CampaignInfo {
    name: string;
    address?: string;
    minimumContribution?: string;
    balance: string;
    requestsCount?: string;
    approversCount?: string;
    manager?: string;
}
